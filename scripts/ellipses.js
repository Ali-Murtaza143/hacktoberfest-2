function setup() {
  createCanvas(displayWidth, displayHeight);
}
  
function draw() {
  fill(Math.floor((Math.random() * 255) + 1));
  ellipse(mouseX, mouseY, 80, 80);
}


function draw() {
  fill(Math.floor((Math.random() * 215) + 1));
  ellipse(mouseX, mouseY, 90, 90);
}
function draw() {
  fill(Math.floor((Math.random() * 315) + 1));
  ellipse(mouseX, mouseY, 100, 100);
}


function draw() {
  fill(Math.floor((Math.random() * 355) + 1));
  ellipse(mouseX, mouseY, 110, 110);
}
function draw() {
  fill(Math.floor((Math.random() * 380) + 1));
  ellipse(mouseX, mouseY, 120, 120);
}
